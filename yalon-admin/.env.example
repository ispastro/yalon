# Supabase — anon key only. This is a browser app; RLS enforces access control.
VITE_SUPABASE_URL=https://ibcebhpglfqbcjmpxdfa.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Express backend — used ONLY for generating signed document URLs
# (that operation requires the service_role key, which must stay server-side).
# Use the bare server origin — no trailing slash, no /api suffix.
VITE_API_BASE_URL=https://yalon.onrender.com
